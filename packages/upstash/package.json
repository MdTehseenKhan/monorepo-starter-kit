{
  "name": "@packages/upstash",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@upstash/ratelimit": "^2.0.3",
    "@upstash/redis": "^1.34.3",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@tooling/typescript": "workspace:*",
    "@types/node": "^20"
  }
}
